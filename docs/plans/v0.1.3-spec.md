# v0.1.3 Spec — Quoted-Key-Cluster Repair + Pre-write YAML Sanity

> **Status:** Draft (Obi 2026-04-28).
> **Triggered by:** GR-2 Cell (4) `property-describe` First Real Run on `nexus-clone-powershell` — F26 quoted-key-pattern-cluster live-confirmed at 5.9% inbox-tree blast-radius (60 of 1016 files), F25 Class-A near-miss caught by Pre-Run-Memo Stop-Bedingung.
> **Prerequisite for:** GR-3 (`nexus-clone-robocopy` Reference-Vault) + GR-4 (M2 Platin production) + Public-Flip. Per heutige Obi-Session-Decision: v0.1.3 ist nicht mehr "fast-follow", sondern **must-ship vor weiteren GR-Runs**.
> **Owner:** Obi for spec; implementer TBD (Bob or Obi) per AD-21 evaluation post-spec-review.

---

## 1. Context

### 1.1 The Bug Surface (F26)

Apple Notes / Obsidian Importer (early-vintage) imports leave YAML frontmatter with **quoted keys that contain a trailing colon inside the quotes**, plus the YAML key-value colon outside:

```yaml
---
"created:": 2024-03-14
"modified:": 2026-04-13
"description:": Original Apple Notes export
tags:
  - AppleNoteImport
---
```

The valid YAML interpretation is: a key literally named `created:` (with the colon as part of the key string) followed by the value. Strict YAML parsers accept this — but **no human author intends this**. It is a serialization artifact from an importer that wrapped keys defensively in quotes and accidentally included the colon.

**Why this bites cross-skill:**

| Skill | Failure mode under quoted-keys |
|-------|-------------------------------|
| `property-enrich` | Step 4 walks Source Hierarchy. Plain-key regex (`^created:\s*`) misses the quoted form, treats `created` as missing, falls through to filesystem birthtime (often clone-fresh = wrong date), **appends a NEW `created:` line below the broken one** → duplicate-key corruption. Live-reproduced GR-2 Cell (1), 60 files. |
| `property-describe` | Step 2 filter regex `^([A-Za-z_][A-Za-z0-9_-]*)\s*:` misses quoted form, treats `description` as missing, **would append a NEW `description:` line** → duplicate-key corruption. Caught by Pre-Run-Memo Stop-Bedingung in GR-2 Cell (4) before write — Class-A near-miss F25. |
| `inbox-sort` | Step 5a already normalizes `"created:"` and `"modified:"` (added 2026-04-27 a per repo issue #6). Other quoted keys (`"description:"`, `"tags:"`) NOT normalized — passes through. |
| `note-rename` | Step 4a already normalizes `"created:"` and `"modified:"` (added 2026-04-27 per repo issue #4). Other quoted keys NOT normalized. |
| `property-classify` (deferred v0.2.0) | Same surface as `property-describe`. Out-of-scope for v0.1.3 but spec'd-in for awareness. |

### 1.2 Cycle-4 Distance Impact

Per heutige Obi-Session expert_notes:

- Original (gestern -e): "3-5 Sessions à 1.5-3h" until USER-PASS.
- Now: v0.1.3-Spec (this) + v0.1.3-Implementation (1-2 sessions) + v0.1.3-Ship + GR-2 Cell-4-re-run + GR-3 (full 4-skill) + GR-4 (full 4-skill) + USER-PASS-pronounce.
- Realistic: **5-7 Sessions, 2-3 weeks pre-Launch**.

This is the right trade-off. Two weeks of v0.1.3 work now > two months of user-corruption-bugs nach Public-Flip.

### 1.3 Scope Discipline

v0.1.3 surface is broad (≥10 backlog items P0/P1 cluster). Per concerns in heutigem Report: spec MUST explicitly prioritize. P0 must-ship items are the launch-blockers. P1 should-ship items defer to v0.1.3.1 / v0.1.4 if Implementation effort runs short.

---

## 2. Scope — P0 Must-Ship

These four items block GR-3 + GR-4 and Public-Flip. Implementation MUST cover all four.

### 2.1 F26-Cluster — Generalized Quoted-Key Normalization (cross-skill)

**Where:** `property-enrich` (new Step 5a), `note-rename` (Step 4a generalize), `inbox-sort` (Step 5a generalize).

**Today (note-rename + inbox-sort):**
> "If the YAML contains `"created:"` or `"modified:"` (quoted with embedded colon), normalize to `created` / `modified` and persist immediately."

Hardcoded key-list. Misses `"description:"`, `"tags:"`, `"title:"`, `"aliases:"`, `"status:"`, `"type:"`, etc.

**v0.1.3 spec — generalized:**
> "If any frontmatter line matches the quoted-key pattern `^\s*"[^"]+:"\s*:` (any key wrapped in double-quotes WITH trailing colon inside quotes), normalize the key by stripping the surrounding `"` and trailing-`:` from inside the quotes. Persist immediately. Repeat scan until no quoted-keys remain (idempotent fixpoint)."

**Per-line regex pattern (safe, single-line input):**

```python
QUOTED_KEY_PATTERN = re.compile(r'^(\s*)"([^"]+):"\s*:(.*)$')
```

**Match groups:**
1. Leading whitespace (preserve indentation)
2. Key name (without surrounding quotes, without trailing-colon-inside-quotes)
3. Everything after the value-colon (the value, possibly with trailing comment)

**Replacement line:** `f"{group1}{group2}:{group3}"` — exact value preserved, only key shape changed.

**Duplicate-key resolution (post-normalization):**

After normalization, two lines may now claim the same key (e.g. broken `"created:": 2024-03-14` + plain `created: 2025-01-01` both become `created:`). Policy:

| Case | Action |
|------|--------|
| Both quoted-original and plain-key existed before normalization | Keep first occurrence (= the quoted-original, normalized) — assume it has the original/correct value. Remove subsequent occurrences. Log as Class-D finding. |
| Two quoted-key forms collide (same key name) | Keep first. Remove second. Log as Class-D finding. |
| No collision (most cases) | Idempotent normalize, no further action. |

**Idempotency check:** Re-running on already-normalized frontmatter is a no-op (no line matches the pattern).

### 2.2 F25 — `property-describe` Filter-Regex Hardening

**Where:** `property-describe` Step 2 (Filter — identify notes needing descriptions).

**Today's regex (inferred from heutige Session — actual shipped code likely uses simpler `description:` substring match):**

```python
# F25 BUG — DO NOT KEEP THIS
DESC_KEY_PATTERN = re.compile(r'^([A-Za-z_][A-Za-z0-9_-]*)\s*:')
```

This matches plain identifier keys. Quoted-key form `"description":` does NOT start with `[A-Za-z_]` (starts with `"`), regex fails → key treated as missing → file flagged eligible → would write duplicate `description:` line.

**v0.1.3 fix — defense-in-depth via two-stage check:**

Step 2 Filter MUST do BOTH:

```python
# Stage A: pre-flight sanity check (per references/yaml-sanity.md)
if has_quoted_keys(frontmatter_lines):
    SKIP file with Class-C finding "broken-yaml: quoted-key pattern detected — run property-enrich first"
    continue

# Stage B: existing description detection (only after sanity OK)
existing_description = find_description_value(frontmatter_lines)
if existing_description and len(existing_description) >= 10:
    SKIP file (already has description)
```

**Why SKIP, not REPAIR:** `property-describe` is "additive only — only writes `description`" per its Boundaries. Quoted-key repair is `property-enrich`'s job (Nahbereich). Maintaining boundaries is safer than introducing cross-skill repair logic.

**User flow:** If `property-describe` skips a file with this finding, user re-runs `property-enrich` → enrich's Step 5a normalizes → user re-runs `property-describe` → now eligible.

### 2.3 F19-LIVE — `property-enrich` New Step 5a (Quoted-Key-Normalize)

**Where:** `property-enrich` Workflow.

**Current Step ordering:**
1. Discover vault
2. Scan
3. Compute (Source Hierarchy)
4. Preview
5. Write
6. Skill Log
7. Findings file
8. Report

**v0.1.3 inserts new Step 5a BETWEEN Compute and Preview:**

```text
5a. Repair corrupted quoted-key variants (Nahbereich, runs before Source Hierarchy fallback).
    - Detect any frontmatter line matching QUOTED_KEY_PATTERN (per § 2.1)
    - Normalize via line-by-line replace (recipe b), preserving value exactly
    - After normalization, re-resolve duplicate-key collisions per § 2.1 policy
    - Log each repair in the report (file_ref + before/after key)
    - This step is mandatory BEFORE the Source Hierarchy walk in step 3 — without it, the Hierarchy falls through to filesystem birthtime on files where YAML had a valid (but broken-keyed) date.
```

**Wait — note-rename Step 4a says "Repair corrupted date-key variants first" BEFORE Step 4b (Source Hierarchy). The numbering convention is:** repair happens before the cooldown evaluation in note-rename / inbox-sort. For property-enrich, repair must happen before the Compute step (which IS the Source Hierarchy walk). So the right insertion point is **before Step 3 (Compute), as a new Step 2a:**

```text
2a. Repair corrupted quoted-key variants (Nahbereich).
    [content as above]
```

**Renumber existing Steps 3-8 to 3-8 (no change) — Step 2a slots cleanly before 3.**

**Edge case — frontmatter with broken date-keys but no other broken keys:** Today's note-rename Step 4a already handles this (since 04-27). v0.1.3 enriches the pattern to ALL quoted-keys.

### 2.4 Pre-Write YAML Sanity Check (Defense-in-Depth)

**New reference doc:** `references/yaml-sanity.md` (full draft in § 5.1 below).

**Where called:** Step-zero of any property-write workflow. ALL 4 launch-scope skills (and future `property-classify`) call it before recipe (b/c/d/e) writes.

**Detection patterns (initial v0.1.3 — extensible later):**

| Pattern | Severity | Repair-Policy | Skip-Policy |
|---------|----------|--------------|-------------|
| Quoted-key `^\s*"[^"]+:"\s*:` | Class-C broken-key | repair via § 2.1 | skip-with-finding (read-only-skills) |
| Multiple frontmatter blocks (≥2 `---/---` pairs) | Class-A corruption | n/a — no repair | skip-with-finding (all skills, route to user-review per note-rename's existing corrupted-file-detection) |
| No frontmatter close (`---` open without close) | Class-A corruption | n/a — no repair | skip-with-finding (all skills) |
| Trailing whitespace-only frontmatter (whitespace between final field and close `---`) | Class-D cosmetic | optional repair | proceed (non-blocking) |

**Per-skill policy decision:**

| Skill | On Class-A | On Class-C broken-key | On Class-D cosmetic |
|-------|-----------|----------------------|--------------------|
| `property-enrich` | skip + finding (route to user) | repair (Step 2a) | proceed |
| `note-rename` | use existing corrupted-file-detection | repair (Step 4a) | proceed |
| `inbox-sort` | skip + finding (route to user) | repair (Step 5a) | proceed |
| `property-describe` | skip + finding | skip + finding (NOT repair — boundaries) | proceed |
| `property-classify` (v0.2.0) | skip + finding | skip + finding | proceed |

Defense-in-depth lives in the **sanity-check call**: skills that already have repair logic in their workflow (enrich, rename, sort) are calling sanity-check as a Step-zero pre-flight. If sanity-check returns Class-A, skill skips. If Class-C, skill calls its own repair step. If both succeed, skill proceeds normally.

---

## 3. Scope — P1 Should-Ship

These items improve robustness but are not launch-blockers. Ship if Implementation has bandwidth; otherwise defer to v0.1.3.1 or v0.1.4.

### 3.1 F18 — Scope-Spec-Drift Unification

**Today:**
- `note-rename` + `inbox-sort` define `scope=inbox` as **root-only explicit** (with `vault` and `folder:<path>` opt-in).
- `property-enrich` + `property-describe` are **vague** (LLM interprets `scope=inbox` as inbox-tree by default).

**Empirically confirmed GR-2 Cell 1:** `scope=inbox` on property-enrich = 514 inbox-tree files vs 108 root-only on inbox-sort GR-1. Cost/token surprise.

**v0.1.3 fix:**

All 4 launch-scope SKILL.mds standardize on:

```yaml
| `scope` | inbox | Which folder to scan. `inbox` = inbox root only. `vault` = entire vault (excluding vault root). `folder:<path>` = specific subfolder. `inbox-tree` = inbox folder including all subfolders (opt-in for bulk-mode). User confirms before execution. |
```

Default = `inbox` = root-only. Recursive opt-in via `inbox-tree`.

### 3.2 F2 — German Date-Format Normalization

**Where:** `property-enrich` Source Hierarchy.

**Today:** Hierarchy tries ISO formats only. German `30.01.2026, 17:02:59` (DACH-region critical) is not parseable → Source-Hierarchy falls through to filesystem birthtime even though a valid date is in YAML.

**v0.1.3 fix:** Add normalization step BEFORE the Hierarchy parser walks ISO formats:

```python
GERMAN_DATE_PATTERN = re.compile(r'^(\d{1,2})\.(\d{1,2})\.(\d{4})(?:,\s*(\d{1,2}):(\d{2})(?::(\d{2}))?)?$')

def normalize_german_date(value: str) -> str | None:
    m = GERMAN_DATE_PATTERN.match(value.strip())
    if not m:
        return None
    day, month, year = m.group(1), m.group(2), m.group(3)
    hour, minute, second = m.group(4), m.group(5), m.group(6)
    iso = f"{year}-{int(month):02d}-{int(day):02d}"
    if hour:
        iso += f"T{int(hour):02d}:{minute}:{second or '00'}"
    return iso
```

Called as first transformation when reading YAML `created` value. If match → use normalized ISO. If no match → fall through to existing parser logic.

### 3.3 F13 — Hybrid-Solution für Skill-Judgment-Instability

**Where:** `note-rename` Corrupted File Detection (Nahbereich).

**Today:** Pure LLM judgment. Same files (`Unbenannt 1` + `Unbenannt`) classified opposite in GR-1 Pass 3 vs GR-2 Cell 2.

**v0.1.3 fix:** Hybrid deterministic-heuristic + user-decision pattern.

**New reference:** `references/judgment-instability.md` with pattern-list. Initial pattern (corrupted-file detection):

```text
Pattern: "two-frontmatter-blocks-detection"

Heuristic (deterministic, runs first):
  Count `---` lines at start-of-file boundaries.
  TWO complete blocks (lines i and j with i < j, both .rstrip() == '---') AND
  Block 1 (lines 0..i) contains NO line matching `^(title|created|modified):` AND
  Block 2 starts after content (line k > j with non-frontmatter content between j and k) AND
  Block 2 is NOT inside a code-fence (no ```yaml or ``` opening before line j+1)
  → suggest: "corrupted (two-block merge)"

User-decision-gate:
  Skill presents heuristic verdict + per-file preview to user.
  User chooses per file: (a) accept verdict (apply corrupted-prefix rename), (b) override (treat as content), (c) skip (TBD-suffix).
  Drift logged: file_ref + heuristic-verdict + user-decision in findings (Class-D).
```

This pattern transfers to other instability classes (e.g. "is this a daily-note?", "is this a web-capture?") — `judgment-instability.md` becomes a growing reference of patterns.

### 3.4 F14-Cluster — Pattern-Detection-Coverage

**Where:** `note-rename` Pass 1 (categorize) — uninformative-name detection.

**Today:** Hard-coded list `Untitled, Unbenannt, New Note, Draft, Blank note, Note from iPhone, Quick Note`. GR-1 Pass 1 missed `Unbenannt`/`Untitled` patterns on three files (caught only in Pass 3 inbox-scope).

**v0.1.3 fix:** Extract patterns to `references/uninformative-names.md` — a configurable list:

```yaml
# uninformative_patterns
en:
  - Untitled
  - "New Note"
  - "Blank note"
  - Draft
  - "Quick Note"
  - "Note from iPhone"
de:
  - Unbenannt
  - "Neue Notiz"
  - Entwurf
es: []
fr: []
# extensible per-language
```

`note-rename` reads this list at runtime, applies all patterns. F9 + F10 (Unbenannt-with-numeric-suffix variants like `Unbenannt 1`, `Unbenannt 2`) become concrete repros in test-fixtures.

### 3.5 F7 — Description-Source-Policy Clarification

**Where:** `property-describe` SKILL.md L43-48 ("How to Write a Good Description").

**Today:** Vague guidance. Cell 4 First Real Run produced one description that referenced URL-slug content (Charly Wargnier + Nate Herkelman in N8n WebCapture LinkedIn-Post-Bookmark) — unclear if URL-encoded text counts as "in-note content".

**v0.1.3 fix:** Add explicit guardrail:

> **Source-of-content policy:** Description MUST be traceable to (a) note body text, (b) URL-encoded text in body links, OR (c) note title. NEVER fabricate or infer beyond these sources. Halluzinations-Audit-Pattern: every claim in description must point at a source token in the note.

Plus add a Quality Check item:

> - [ ] Every description claim is traceable to body, URL-text, or title (no fabrication)

### 3.6 Test-Fixture Expansion

**Where:** `test-data/`.

**Today:** `f8-repro.md`, `f15-repro.md` shipped in v0.1.2.

**v0.1.3 add:**
- `test-data/f19-repro.md` — file with `"created:"` + `"modified:"` quoted keys, no plain `created`/`modified` lines. Verifies Step 5a normalize.
- `test-data/f25-repro.md` — file with `"description":` quoted-key + valid description value. Verifies property-describe Step 2 SKIPS.
- `test-data/f26-repro.md` — file with mixed quoted keys (`"created:"`, `"modified:"`, `"description":`, `"tags":`) AND a parallel `created:` plain key (duplicate-collision). Verifies cross-skill cluster handling + duplicate-resolution.
- `test-data/f2-repro.md` — file with German date format `created: 30.01.2026, 17:02:59`. Verifies property-enrich Source Hierarchy German-date normalization.

Full content drafts in § 6.

### 3.7 `scripts/smoke-test.sh` — Automated Runner

**Where:** new `scripts/` directory.

**Today:** No automated test runner. Test fixtures are documentation only.

**v0.1.3 add:** Shell script that:
1. Copies `test-data/*.md` into a temp vault (`/tmp/vault-autopilot-smoke/`)
2. Sets `OBSIDIAN_VAULT_PATH` to temp vault
3. Runs each launch-scope skill in declared order (enrich → rename → sort → describe)
4. After each skill, diffs the temp-vault state against `test-data/expected/<skill>/` golden files
5. Fails on diff (exit 1), succeeds on clean (exit 0)
6. Cleans up temp vault on completion

Full design in § 7.

---

## 4. Out-of-Scope — Defer to v0.1.3.1 / v0.1.4

| ID | Item | Defer reason |
|----|------|-------------|
| F17 | `expected_count` parameter for property-* + inbox-sort | P3 — pre-Run-Memo workaround exists |
| F20 | User-Override-Encoding-Mismatch (PowerShell-Hashtable Umlaute) | P2 — not yaml-edit recipe issue, separate Override-system; Cell 4 confirmed yaml-edits.md recipes ARE encoding-safe |
| F23 | 2-sentence threshold validation | P3 — NOT-confirmed positive signal, no fix needed |
| Token-cost instrumentation | P3 — v0.2.0 roadmap |
| T52 | Discoverability obi-startup-checklist update | P2 — works manually post-v0.1.2 ledger |

These items go into `docs/backlog/ideas.md` v0.1.3.1 / v0.1.4 section after spec acceptance. Not in this PR.

---

## 5. New Reference Documents

### 5.1 `references/yaml-sanity.md` (full draft)

```markdown
# YAML Sanity — Pre-Write Defense-in-Depth

## Purpose

Every property-writing skill calls this sanity-check BEFORE attempting any
YAML edit (recipes b/c/d/e per `references/yaml-edits.md`). The check returns
a verdict the skill uses to decide: proceed, repair, or skip-with-finding.

## When to call

Step zero of any property-write workflow:
- `property-enrich` Step 2a (before Compute / Source Hierarchy)
- `property-describe` Step 2 (before Filter — eligibility check)
- `note-rename` Step 4a (before Classify cooldown logic)
- `inbox-sort` Step 5a (before Cooldown evaluation)
- `property-classify` (v0.2.0+) Step 2 (before Type/Status detection)

## Procedure

1. Read frontmatter lines per `references/yaml-edits.md` recipe (a).
2. If no frontmatter exists: return verdict `OK_NO_FRONTMATTER`.
3. Walk frontmatter lines. Match each line against detection patterns (§ 3
   below). Collect findings.
4. Return verdict + finding list.

## Verdicts

| Verdict | Meaning | Action by skill |
|---------|---------|-----------------|
| `OK` | Frontmatter is well-formed, no detected issues | Proceed normally |
| `OK_NO_FRONTMATTER` | File has no frontmatter to inspect | Proceed normally — recipe (c) creates one if needed |
| `BROKEN_KEYS` | One or more quoted-key patterns detected | Repair (if skill is repair-capable) OR skip + Class-C finding |
| `MULTIPLE_FRONTMATTER_BLOCKS` | Two or more `---/---` pairs at frontmatter-boundary positions | Skip + Class-A finding. Route to `note-rename` corrupted-file-detection if not already there. |
| `UNCLOSED_FRONTMATTER` | First line `---` but no matching closing `---` | Skip + Class-A finding. User-investigation required. |
| `INVALID_YAML` | Detection patterns OK but YAML still unparseable (rare) | Skip + Class-A finding. Manual review. |

## Detection patterns

### Pattern 1 — Quoted-key with embedded colon

**Per-line regex (single-line input — safe per yaml-edits.md):**

```python
QUOTED_KEY_PATTERN = re.compile(r'^(\s*)"([^"]+):"\s*:(.*)$')
```

**Match:** indent + key-without-quotes-or-trailing-colon + value-after-colon.

**Example:**
```
"created:": 2024-03-14
```
Match groups: `("", "created", " 2024-03-14")`.

**Verdict contribution:** if any line matches → `BROKEN_KEYS`.

### Pattern 2 — Multiple frontmatter blocks

Walk the entire file (not just frontmatter). Count occurrences of lines where
`.rstrip() == '---'`. Treat first occurrence at line 0 as frontmatter open.
Treat next occurrence as frontmatter close.

After the close, if a subsequent line is also `.rstrip() == '---'` AND is NOT
inside a code-fence (no `\`\`\`` opening before it), AND a subsequent matching
`---` exists: this is a SECOND frontmatter block.

**Verdict contribution:** if two or more frontmatter blocks → `MULTIPLE_FRONTMATTER_BLOCKS`.

This pattern is canonically defined in `note-rename`'s Corrupted File Detection
(SKILL.md § "Corrupted File Detection"). The sanity-check uses the same
detection logic, exposed as a callable.

### Pattern 3 — Unclosed frontmatter

If line 0 is `---` but no subsequent line equals `---`: frontmatter never
closes. **Verdict:** `UNCLOSED_FRONTMATTER`.

### Pattern 4 — Cosmetic-only (Class-D)

Whitespace-only lines between final field and closing `---`. Or trailing
whitespace on a frontmatter field line. These are non-blocking.

**Verdict contribution:** logged in finding list, but verdict stays at
the worst non-cosmetic verdict found.

## Repair eligibility

| Skill | Verdict response |
|-------|-----------------|
| `property-enrich` | OK → proceed. BROKEN_KEYS → repair via Step 2a, then re-run sanity-check (idempotent fixpoint). MULTIPLE_FRONTMATTER_BLOCKS → skip + Class-A finding. UNCLOSED → skip + Class-A. |
| `note-rename` | OK → proceed. BROKEN_KEYS → repair via Step 4a (existing). MULTIPLE_FRONTMATTER_BLOCKS → use existing Corrupted File Detection (rename file with corruption-label). |
| `inbox-sort` | OK → proceed. BROKEN_KEYS → repair via Step 5a. MULTIPLE_FRONTMATTER_BLOCKS → skip + Class-A finding (route to note-rename). |
| `property-describe` | OK → proceed. BROKEN_KEYS → SKIP + Class-C finding (boundaries: describe is additive-only). MULTIPLE_FRONTMATTER_BLOCKS → skip + Class-A. |
| `property-classify` (v0.2.0) | Same as property-describe. |

## Idempotency

After a repair-skill runs § 5.1 quoted-key normalization, calling the sanity-check
again on the same file MUST return `OK` (or another non-`BROKEN_KEYS` verdict).
This is the contract: repair is permanent within the run, normalize is idempotent.

## Worked example

Input file (broken):
```yaml
---
"created:": 2024-03-14
"description:": Apple Notes export
tags: [AppleNoteImport]
---
```

`property-enrich` calls sanity-check:
1. Walk lines 1-3.
2. Line 1: matches QUOTED_KEY_PATTERN → finding ("created").
3. Line 2: matches QUOTED_KEY_PATTERN → finding ("description").
4. Line 3: no match.
5. Return `BROKEN_KEYS` with two findings.

Skill calls Step 2a normalize:
1. Replace line 1: `created: 2024-03-14`
2. Replace line 2: `description: Apple Notes export`

Re-call sanity-check: returns `OK`. Skill proceeds.

`property-describe` later runs on same file (now repaired): sanity-check returns
`OK`. Filter detects `description: Apple Notes export` (length 20 ≥ 10) → file
already has description → skip (eligible-skip, not error-skip).

## Why a separate file

`yaml-edits.md` defines the WRITE recipes (b, c, d, e) and the prerequisite
read recipe (a). `yaml-sanity.md` defines the PRE-WRITE check that runs before
any of those recipes. They are complementary: yaml-edits.md is "how to safely
edit", yaml-sanity.md is "should we attempt to edit at all".
```

### 5.2 `references/yaml-edits.md` Addendum — Recipe (f)

Add new recipe to existing file:

```markdown
## Recipe (f) — Normalize quoted-keys (repair pass)

This is the F26 cluster surface. Read it twice.

```text
1. Read frontmatter lines (recipe a). If no frontmatter, no-op.
2. Walk frontmatter lines with index. For each line where the per-line regex
   QUOTED_KEY_PATTERN = re.compile(r'^(\s*)"([^"]+):"\s*:(.*)$') matches:
   a. Extract groups: indent, key_name, value_with_colon_separator.
   b. Replace line with: f"{indent}{key_name}:{value_with_colon_separator}".
3. After all replacements, walk frontmatter lines again. If any key-name now
   appears on two or more lines (post-normalization collision):
   a. Keep the FIRST occurrence (= the line that was originally quoted, now
      normalized — assume original/correct value).
   b. Remove subsequent occurrences.
   c. Log each removed line as Class-D finding (file_ref + key_name + value).
4. Idempotency: if no lines matched in step 2, the function is a no-op.
   Re-running on already-normalized frontmatter returns unchanged.
5. Write frontmatter + body back per recipe (a) write-back semantics.
```

## DO NOT — broken normalize patterns

```python
# DO NOT WRITE THIS — multi-line regex over joined frontmatter
content = re.sub(
    r'(?ms)"([^"]+):"\s*:\s*(.+?)\n',
    lambda m: f'{m.group(1)}: {m.group(2)}\n',
    content,
)
# Why it fails: under (?s), `.+?` matches across newlines (non-greedy still
# matches newlines). The lazy quantifier saves you from greedy-eat-everything
# but YAML quoted-string values can themselves span newlines — corrupting
# multi-line values. Per-line regex is fine. Multi-line regex is not.
```

Use recipe (f) as defined. Walk lines. Per-line regex match. Replace per line.
```

### 5.3 `references/judgment-instability.md` (P1, optional v0.1.3 — see § 3.3)

Pattern catalog for cases where pure LLM judgment produces opposite verdicts on
identical inputs. Each pattern has:
- A deterministic heuristic (for the suggest-step)
- A user-decision-gate (preview + per-file override)
- A drift-log (Class-D finding with file_ref + heuristic-verdict + user-decision)

Initial v0.1.3 entry: corrupted-file-detection (per § 3.3). Future patterns
added as they surface (web-capture-detection, daily-note-classification, etc.).

### 5.4 `references/uninformative-names.md` (P1, optional v0.1.3 — see § 3.4)

Per-language list of filename patterns that trigger `note-rename` candidacy.
Replaces hardcoded SKILL.md list. Format: YAML, language-keyed.

---

## 6. Test Fixtures (full content drafts)

Saved at `test-data/<id>-repro.md`. Each fixture is a runnable input file. The
expected output (post-skill-run) lives in `test-data/expected/<skill>/<id>-repro.md`.

### 6.1 `test-data/f19-repro.md`

```yaml
---
"created:": 2024-03-14
"modified:": 2024-03-14
title: F19 Quoted-Key Repro
tags:
  - AppleNoteImport
---

# F19 Quoted-Key Repro

This file reproduces F19. property-enrich should detect the quoted-keys via
sanity-check, normalize via Step 2a (recipe f), then proceed normally.

Expected post-enrich frontmatter:

```yaml
---
created: 2024-03-14
modified: 2024-03-14
title: F19 Quoted-Key Repro
tags:
  - AppleNoteImport
  - VaultAutopilot
---
```

(plus skill-log callout appended)
```

### 6.2 `test-data/f25-repro.md`

```yaml
---
created: 2024-03-14
"description:": A pre-existing description with broken-key shape
tags:
  - AppleNoteImport
---

# F25 Filter-Regex Repro

This file reproduces F25. property-describe Step 2 must SKIP this file
(broken-key detected) — NOT write a duplicate `description:` line.

Pre-v0.1.3 (buggy): describe would not see the existing description and
would append a new `description:` line below the broken one → duplicate-key
corruption.

Post-v0.1.3 (fixed): describe sanity-check detects quoted-key →
SKIP with Class-C finding "broken-yaml: quoted-key pattern detected — run
property-enrich first".

After running property-enrich first (which normalizes via Step 2a), re-run
property-describe: file now has `description: A pre-existing description...`
(length > 10) → skipped as "already has description" (eligible-skip).
```

### 6.3 `test-data/f26-repro.md`

```yaml
---
"created:": 2024-03-14
created: 2025-01-01
"modified:": 2024-06-15
"description:": Apple Notes export
"tags:": []
---

# F26 Cross-Skill Cluster Repro

This file reproduces F26 with multiple quoted-keys AND a duplicate-collision
(both `"created:"` and `created` exist).

Expected behavior on property-enrich Step 2a:

1. Walk lines, find all four quoted-key lines.
2. Normalize: `"created:"` → `created`, `"modified:"` → `modified`,
   `"description:"` → `description`, `"tags:"` → `tags`.
3. Duplicate-collision: TWO `created:` lines now exist (original quoted at
   line 1, original plain at line 2).
4. Per § 2.1 policy: keep first (= the normalized-from-quoted), remove second.
   Log Class-D finding "duplicate-key removed: created (kept original
   quoted-form value 2024-03-14, removed plain-form value 2025-01-01)".

Expected post-enrich frontmatter:

```yaml
---
created: 2024-03-14
modified: 2024-06-15
description: Apple Notes export
tags: []
---
```

Note: empty `tags: []` becomes block-format `tags:\n  - VaultAutopilot` after
skill-log Nahbereich (per skill-log.md tag-format-rules — convert inline `[]`
to block).
```

### 6.4 `test-data/f2-repro.md`

```yaml
---
created: 30.01.2026, 17:02:59
modified: 31.01.2026
title: F2 German-Date Repro
---

# F2 German-Date Repro

This file reproduces F2 (German DD.MM.YYYY[, HH:mm:ss] format).

Pre-v0.1.3 (buggy): property-enrich Source Hierarchy doesn't parse the German
format → falls through to filesystem birthtime (clone-fresh date wrong) →
`created` would be appended-as-new-line below the unparseable existing one
(duplicate-key corruption) OR overwritten silently.

Post-v0.1.3 (fixed): German-date normalization in Source Hierarchy produces
ISO `2026-01-30T17:02:59` for `created`. Existing field is preserved
(additive-only). `modified` is refreshed via filesystem mtime (per existing
SKILL spec — `modified` is the only Always-overwrite field).
```

### 6.5 `test-data/expected/` golden files

Each fixture has a corresponding `expected/` file under the relevant skill.
Smoke-test runner diffs actual vs expected.

```
test-data/
  f8-repro.md         (existing, v0.1.2)
  f15-repro.md        (existing, v0.1.2)
  f19-repro.md        (new)
  f25-repro.md        (new)
  f26-repro.md        (new)
  f2-repro.md         (new)
  expected/
    property-enrich/
      f19-repro.md    (post-enrich state)
      f26-repro.md    (post-enrich state)
      f2-repro.md     (post-enrich state)
    property-describe/
      f25-repro.md    (post-describe state — should be byte-identical to input, since SKIP)
    inbox-sort/
      f8-repro.md     (post-sort state)
    note-rename/
      (existing fixtures TBD)
```

---

## 7. `scripts/smoke-test.sh` Design

### 7.1 Goal

Detect regression of any v0.1.2 + v0.1.3 fix without manual GR-runs. Runs
locally on macOS (the dev environment). Cross-platform later (v0.2.0+).

### 7.2 Constraints

- Must NOT depend on actual Claude Code runtime — skills are markdown specs,
  not executable code. Smoke-test verifies the **input/output contract**, not
  the LLM execution path.
- Must not have external dependencies beyond standard macOS shell tools (bash,
  diff, mkdir, cp, rm).
- Exit 0 on full pass, exit 1 on first diff.

### 7.3 Architecture

The runner is a contract-validator, not an LLM-executor. It:

1. Sets up a temp vault with copies of `test-data/*.md`.
2. For each fixture, the runner expects a parallel `test-data/expected/<skill>/<fixture>.md` file documenting the post-skill state.
3. The runner does NOT actually invoke Claude Code or any LLM. Instead, it provides a manual workflow for the developer:
   - Print the temp-vault path
   - Print the next test-step (e.g. "Run `property-enrich` against `<temp-vault>` with `scope=vault`")
   - Wait for user to indicate done (`y` to continue)
   - Diff temp-vault state against `test-data/expected/<skill>/`
   - Report diff result

This is a "harness for manual GR-runs against fixtures" rather than full
automation. v0.2.0+ may add LLM-driven automation if Claude Agent SDK supports
it cleanly.

### 7.4 Script outline

```bash
#!/usr/bin/env bash
# scripts/smoke-test.sh
# Manual harness for v0.1.x test fixtures. Sets up temp vault, walks fixtures,
# diffs against expected golden files. Exits 1 on first diff.

set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
TEMP_VAULT="$(mktemp -d /tmp/vault-autopilot-smoke.XXXXXX)"
trap 'rm -rf "$TEMP_VAULT"' EXIT

echo "=== smoke-test ==="
echo "Repo root:  $REPO_ROOT"
echo "Temp vault: $TEMP_VAULT"
echo

# 1. Copy fixtures to temp vault
mkdir -p "$TEMP_VAULT/Inbox"
cp "$REPO_ROOT/test-data"/*.md "$TEMP_VAULT/Inbox/"
echo "Copied $(ls "$REPO_ROOT/test-data"/*.md | wc -l) fixtures to Inbox/"
echo

# 2. Walk skills in declared order
SKILLS=(property-enrich note-rename inbox-sort property-describe)

for SKILL in "${SKILLS[@]}"; do
  echo "--- Step: $SKILL ---"
  echo "Run \`$SKILL\` against \`$TEMP_VAULT\` (scope=inbox)."
  echo "Press Enter when done (or Ctrl-C to abort)..."
  read -r

  # 3. Diff against expected golden files
  EXPECTED_DIR="$REPO_ROOT/test-data/expected/$SKILL"
  if [[ ! -d "$EXPECTED_DIR" ]]; then
    echo "  No expected/ directory for $SKILL — skipping diff"
    continue
  fi

  for EXPECTED in "$EXPECTED_DIR"/*.md; do
    FIXTURE="$(basename "$EXPECTED")"
    ACTUAL="$TEMP_VAULT/Inbox/$FIXTURE"
    if [[ ! -f "$ACTUAL" ]]; then
      # File may have been moved by inbox-sort — search recursively
      ACTUAL="$(find "$TEMP_VAULT" -name "$FIXTURE" -type f | head -1)"
    fi
    if [[ ! -f "$ACTUAL" ]]; then
      echo "  FAIL: $FIXTURE not found in temp vault after $SKILL"
      exit 1
    fi
    if ! diff -u "$EXPECTED" "$ACTUAL" > /tmp/smoke-diff.txt; then
      echo "  FAIL: $FIXTURE differs from expected"
      cat /tmp/smoke-diff.txt
      exit 1
    fi
    echo "  PASS: $FIXTURE matches expected"
  done
done

echo
echo "=== smoke-test PASS ==="
```

### 7.5 Limitations

- Manual: developer runs each skill against the temp vault, then presses Enter. Real automation requires Claude Agent SDK integration (v0.2.0+).
- Skill-log callout includes timestamps — exact byte-match diff fails. Solution: golden files use placeholders (`{{TIMESTAMP}}`) and the diff filters those before comparing.
- Birthtime preservation tests need filesystem-level inspection (stat -f %B) — not in initial smoke-test.

These limitations are acceptable for v0.1.3. The smoke-test catches regression in the YAML-edit recipes (the F8/F15/F19/F25/F26 surface) — which is the bug-class v0.1.x is fighting.

---

## 8. Per-Skill Implementation Patches

Concrete edits for the implementer. These are diffs against current `main` (= post-PR-#10 v0.1.2 state).

### 8.1 `skills/property-enrich/SKILL.md`

**Insert new Step 2a (between current Step 2 "Scan" and Step 3 "Compute"):**

```markdown
   - **2a. Repair corrupted quoted-key variants first (Nahbereich, sanity-check).** Call `references/yaml-sanity.md` for each scanned note. If verdict is `BROKEN_KEYS`, normalize via `references/yaml-edits.md` recipe (f). After normalization, resolve duplicate-key collisions per recipe (f) policy. If verdict is `MULTIPLE_FRONTMATTER_BLOCKS` or `UNCLOSED_FRONTMATTER`, skip the file and log Class-A finding. This step is mandatory BEFORE Step 3 (Compute / Source Hierarchy walk) — without it, the Hierarchy falls through to filesystem birthtime on files where YAML had a valid (but broken-keyed) date.
```

**Update Step 5 (Write) preamble:**

```markdown
5. **Write** — add fields using line-by-line YAML edits per `references/yaml-edits.md`. Pre-write, call `references/yaml-sanity.md` again as defense-in-depth (sanity-check is idempotent — repeated calls are no-ops if Step 2a already normalized). New fields are inserted as new lines immediately before the closing `---` (recipe c). List-field appends (e.g. `tags:`) follow recipe d. Preserve all existing field values.
```

**Add to Quality Check:**

```markdown
- [ ] Sanity-check called pre-Compute (Step 2a) and pre-Write (Step 5)
- [ ] Quoted-key broken-key variants normalized via recipe (f), not appended-below
- [ ] Duplicate-key collisions resolved (first-occurrence-wins, subsequent removed and logged)
```

**F2 German-date addendum to Source Hierarchy (Prio 2 — Filename, Prio 3 — Git, Prio 4 — Birthtime, but as preprocessing on Prio 1 — YAML existing value):**

```markdown
### `created` Source Hierarchy (v0.1.3 update)

When `created` is missing from YAML, derive it from the highest-priority available source. Prio 1 includes a normalization preprocessing step for non-ISO formats:

| Prio | Source | How | When reliable |
|------|--------|-----|---------------|
| 1 | YAML `created` exists | If value parses as ISO 8601 → use directly. Else try German `DD.MM.YYYY[, HH:mm:ss]` normalization (per `references/german-date-normalization.md`). Else flag as Class-C "unparseable date format" finding and fall through to Prio 2. | Always, with v0.1.3 German-date support |
| 2 | Filename date pattern | Parse `YYYY-MM-DD` from filename | When user names files with dates |
| 3 | Git first-commit timestamp | `git log --follow --diff-filter=A --format=%aI -- <file>` | When vault is under Git |
| 4 | Filesystem birthtime (last resort) | `stat -f %SB` (macOS) / `stat -c %W` (Linux) | Only on native (non-cloned) vaults |
```

### 8.2 `skills/property-describe/SKILL.md`

**Insert new Step 2a (between Step 2 "Filter" and Step 3 "Generate"):**

Wait — Step 2 IS the filter. Better: prepend a sub-step inside Step 2.

**Replace current Step 2 (Filter):**

```markdown
2. **Filter** — for each note in scope:
   - **2a. Pre-flight sanity-check.** Call `references/yaml-sanity.md`. If verdict is `BROKEN_KEYS`, `MULTIPLE_FRONTMATTER_BLOCKS`, or `UNCLOSED_FRONTMATTER`: SKIP with Class-C (broken-keys) or Class-A (corruption) finding. property-describe is additive-only — quoted-key repair is property-enrich's job. The user re-runs property-enrich first, then property-describe.
   - **2b. Eligibility check.** Use plain-key match for `description:`. Note: after Step 2a, frontmatter is guaranteed normalized — plain-key match is now sufficient and safe. Identify notes needing descriptions (missing/placeholder/too-thin per "Which Notes Get a Description" rules).
```

**Update Step 5 (Write) preamble (analogous to enrich):**

```markdown
5. **Write** — set `description` in YAML frontmatter. Pre-write, call `references/yaml-sanity.md` again as defense-in-depth. Line-by-line replacement only (recipe b). Preserve all other fields. Single-quote the value, escape apostrophes by doubling.
```

**Add to Quality Check:**

```markdown
- [ ] Sanity-check called pre-Filter (Step 2a) and pre-Write (Step 5)
- [ ] Quoted-key broken-key files SKIPPED with Class-C finding (not repaired, not written)
```

**F7 source-policy addendum to "How to Write a Good Description":**

```markdown
- **Source-of-content policy:** Every claim in the description MUST be traceable to (a) note body text, (b) URL-encoded text in body links, OR (c) note title. NEVER fabricate or infer beyond these sources. Halluzinations-Audit-Pattern: every claim points at a source token in the note.
```

**Add to Quality Check (F7):**

```markdown
- [ ] Every description claim is traceable to body, URL-text, or title (no fabrication)
```

### 8.3 `skills/note-rename/SKILL.md`

**Generalize Step 4a (replace existing wording):**

```markdown
   - **4a. Repair corrupted quoted-key variants first (Nahbereich, sanity-check).** Call `references/yaml-sanity.md`. If verdict is `BROKEN_KEYS`, normalize via recipe (f) — handles ALL quoted-key patterns, not just `"created:"`/`"modified:"` (broadened from v0.1.0/v0.1.2 hardcoded list). After normalization, resolve duplicate-key collisions per recipe (f) policy. If verdict is `MULTIPLE_FRONTMATTER_BLOCKS`, use existing Corrupted File Detection (rename file with corruption-label). YAML edits MUST follow `references/yaml-edits.md` (recipe b for plain replace, recipe f for quoted-key normalize). Without this normalization a strict YAML parser cannot read the author-intended date, falls back to the Source Hierarchy → filesystem birthtime (often fresh on cloned vaults), and the cooldown evaluation in 4c silently skips legitimate candidates. Historical bug: repo issue #4 (2026-04-27) for `created`/`modified`; F26 cross-skill cluster (2026-04-28) generalized the pattern.
```

**Add to Quality Check:**

```markdown
- [ ] Sanity-check called Step 4a (broadened from hardcoded `"created:"`/`"modified:"` to all quoted-keys)
```

### 8.4 `skills/inbox-sort/SKILL.md`

**Generalize Step 5a (replace existing wording, analogous to note-rename):**

```markdown
   - **5a. Repair corrupted quoted-key variants first (Nahbereich, sanity-check).** Call `references/yaml-sanity.md`. If verdict is `BROKEN_KEYS`, normalize via recipe (f) — handles ALL quoted-key patterns (broadened from v0.1.2 hardcoded `"created:"`/`"modified:"`). After normalization, resolve duplicate-key collisions per recipe (f) policy. If verdict is `MULTIPLE_FRONTMATTER_BLOCKS` or `UNCLOSED_FRONTMATTER`, skip the file and log Class-A finding (route to note-rename for handling). YAML edits MUST follow `references/yaml-edits.md` (recipes b + f). Mirrors note-rename Step 4a. Historical bug: repo issues #4 and #6 (2026-04-27) for `created`/`modified`; F26 cross-skill cluster (2026-04-28) generalized.
```

**Add to Quality Check:**

```markdown
- [ ] Sanity-check called Step 5a (broadened from hardcoded `"created:"`/`"modified:"` to all quoted-keys)
```

### 8.5 F18 Scope-Spec-Drift (P1) — all 4 SKILLs

Update the `scope` parameter table row in all 4 SKILL.mds:

```markdown
| `scope` | inbox | Which folder to scan. `inbox` = inbox root only (default). `inbox-tree` = inbox folder including all subfolders (opt-in for bulk-mode, e.g. initial vault setup). `vault` = entire vault excluding root. `folder:<path>` = specific subfolder. User confirms before execution. |
```

property-enrich + property-describe today are vague — this PR makes them explicit, default-aligned with note-rename + inbox-sort.

### 8.6 F14 Pattern-Detection-Coverage (P1) — `note-rename`

Update Rename Candidates section to reference the external list:

```markdown
## Rename Candidates

Rename notes with **uninformative** filenames per `references/uninformative-names.md` (per-language, configurable). Defaults include English (`Untitled`, `New Note`, `Blank note`, `Draft`, `Quick Note`, `Note from iPhone`) and German (`Unbenannt`, `Neue Notiz`, `Entwurf`). URL-only names, hash-only names, and obvious typos (95%+ clear intent) also rename-eligible.

**Never rename:** Already descriptive names.
```

---

## 9. Implementation Roadmap

### 9.1 Sequence (must be ordered)

1. **References first** — `yaml-sanity.md` (new) + `yaml-edits.md` recipe (f) addendum + `german-date-normalization.md` (new for F2) + `uninformative-names.md` (new for F14, P1) + `judgment-instability.md` (new for F13, P1).
2. **SKILL.md updates** — all 4 launch-scope skills, in any order (no inter-skill code dependencies).
3. **Test fixtures** — `f19/f25/f26/f2-repro.md` + `expected/<skill>/*.md` golden files.
4. **smoke-test.sh runner** — depends on test fixtures.
5. **plugin.json + marketplace.json bump** — 0.1.2 → 0.1.3.
6. **logs/changelog.md update** — release notes.
7. **ROADMAP.md update** — move v0.1.3 to "current", v0.1.2 to "previous".

### 9.2 Effort estimate

| Block | Effort | Notes |
|-------|--------|-------|
| § 5.1 yaml-sanity.md | M (~600 lines) | Defense-in-depth doc, complex |
| § 5.2 yaml-edits.md recipe (f) | S (~80 lines added) | Mechanical addition |
| § 8.1 property-enrich Step 2a + F2 | S | Insert new step, German-date helper |
| § 8.2 property-describe Step 2a + F7 | S | Insert pre-flight, source-policy guardrail |
| § 8.3 + 8.4 rename + sort generalize | XS | Reword existing Step 4a/5a |
| § 8.5 scope unification (P1) | XS | One-line table updates × 4 |
| § 8.6 uninformative-names (P1) | S | Extract list, ref doc, SKILL update |
| § 6 test fixtures | S | 4 fixture files + 4-6 expected files |
| § 7 smoke-test.sh | S (~80 lines) | Bash, manual harness |
| Version bumps + changelog + ROADMAP | XS | Mechanical |
| **Total P0 only** | **L** (~6-8h, one focused session) | |
| **Total P0 + P1** | **L+** (~8-12h, 1-2 sessions) | |

### 9.3 Branching strategy

- This spec lands as PR #1 from `obi/2026-04-28-v0.1.3-spec` (just adds `docs/plans/v0.1.3-spec.md`).
- Implementation lands as PR #2 from `bob/2026-04-29-v0.1.3-impl` OR `obi/2026-04-29-v0.1.3-impl` (per AD-21 evaluation post-spec-review by user).
- If P1 items split out: PR #3 as `*/2026-XX-XX-v0.1.3.1` from continuation branch.

### 9.4 Validation gates

Before declaring v0.1.3 ready to ship:

1. All P0 items implemented (§ 2.1–2.4).
2. `scripts/smoke-test.sh` passes against new fixtures (manual run on macOS).
3. SKILL.md Quality Checks updated and verified.
4. `references/yaml-sanity.md` reviewed by user (defense-in-depth doc — security-adjacent).
5. v0.1.3-PR opened, CI green, user-merged.
6. **GR-2 Cell 4 re-run** on `nexus-clone-powershell` with same random.seed (20260428) for vorher/nachher comparability — verifies F26-cluster reduction in skip-rate.
7. Plus 5-10 additional random files from the 60 F26-Cluster-paths (path-list at `<vault>/_vault-autopilot/findings/2026-04-28-quoted-key-blast-radius.txt`) — verifies broken-key repair end-to-end.

Only after all 7 gates → GR-3 + GR-4 unblock.

---

## 10. Pattern-Refinement Open Question (F6)

Per heutigem Report concerns: F26 cluster-pattern is "sub-population of Apple-Notes-Imports", but Files 1+5 of Cell 4 sample have `AppleNoteImport` tag AND clean frontmatter. Naive "all imports are broken" is wrong.

**v0.1.3 Spec-Session recommendation:** Inspect 5-10 of the 60 F26-cluster files (path-list available on ThinkCentre at `<vault>/_vault-autopilot/findings/2026-04-28-quoted-key-blast-radius.txt`). Goal: identify common signature (import-vintage timestamp range, common tag combination, common path prefix) that distinguishes broken-key-imports from clean imports.

If such a signature exists → test-fixtures can mimic the signature for higher-fidelity testing. If no signature exists → the only-detectable feature IS the quoted-key pattern itself, and the test-fixtures (§ 6) cover that already.

**Decision-deferral:** This pattern-refinement is a v0.1.3-Spec-Session-side-investigation (1-2h on ThinkCentre, user-side). NOT blocking implementation — fixtures + smoke-test cover the regression-surface either way. If the investigation surfaces additional patterns, they go into v0.1.3.1.

---

## 11. Marketing-Communication Awareness

Per heutigem Report recommendations: Gary's 2026-04-23b posts shipped with 4-Skill launch-scope (`note-rename + inbox-sort + property-enrich + property-describe`). v0.1.3 keeps this scope intact — F26-Fix in describe + enrich + rename + sort makes the 4-skill launch story whole.

**Worst-case path** (NOT recommended): if Implementation runs catastrophically long and `property-describe` cannot land a stable F25 fix in v0.1.3, then describe defers to v0.1.3.1 and Gary's posts need a "3-skill launch + describe coming next week" amendment. Spec-Session estimate (8-12h L+) suggests this worst-case is unlikely — but if it happens, the comm-update is owned by Gary (HQ-side), not by this PR.

**Recommendation:** v0.1.3 keeps describe in launch-scope via F26-Fix + F25-Fix + sanity-check. Gary's posts remain unchanged.

---

## 12. Out-of-Scope Reminders

- This is the **spec**. Implementation is a separate PR. This file is read by the implementer (Bob or Obi) at start-of-implementation, treated as canonical.
- Master Plan §3 / §4 / §5 / §7 updates remain MASCHIN's job (T11 in heutigem Obi-Report). This spec does NOT modify Master Plan — it is the input to Master Plan §3 Risk-Register update for F26.
- Vault-side `_vault-autopilot.md` Dashboard drift (T12) is user-side cleanup, unrelated to this spec.
- F20 (Override-Encoding-Mismatch) is deferred per § 4. Today's investigation showed the yaml-edits.md recipes ARE encoding-safe — F20 is a separate Override-Hashtable issue, not a yaml-edit issue.

---

## 13. Risks

| Risk | Likelihood | Mitigation |
|------|-----------|-----------|
| Spec misses a quoted-key variant we haven't seen yet | Medium | Per-line regex matches the general shape `"<anything>:"\s*:`. Variants discovered later → test-fixtures + recipe (f) idempotent fixpoint handles them automatically. |
| Sanity-check turns out too aggressive (false-positive Class-A on legitimate frontmatter shapes) | Low | Conservative detection: Class-A only on multi-frontmatter-blocks (clear corruption) and unclosed-frontmatter (clear corruption). Class-C on quoted-keys (always wrong, always repairable). |
| smoke-test.sh manual-mode is too friction-ful, devs skip it | Medium | Document in CONTRIBUTING.md as required-pre-PR-step. v0.2.0+ may automate via Claude Agent SDK. |
| German-date normalization conflicts with a niche locale's date format | Low | Scope: ONLY `DD.MM.YYYY` Pattern. Other locales (e.g. American `MM/DD/YYYY`) NOT addressed in v0.1.3 — would be separate spec items. |
| Implementation effort balloons to 2-3 sessions instead of 1-2 | Medium | P1 items (§ 3) deferrable to v0.1.3.1. Hard time-box on P0 only first. |
| Spec contains errors that surface only in implementation | Medium | Spec-review by user before implementation kickoff (recommended). Plus implementation-session has TodoWrite + AskUserQuestion to flag spec-divergence. |

---

## 14. Acceptance

This spec is accepted when:

1. User reviews + acknowledges § 2 (P0 scope) is correct and sufficient.
2. User confirms § 3 (P1 scope) priority order — items can be re-ordered or split to v0.1.3.1.
3. User confirms § 4 (defer list) is acceptable for v0.1.3 boundary.
4. User confirms § 9.4 (validation gates) — especially the GR-2 Cell 4 re-run requirement before GR-3 unblock.

After acceptance: implementer (Bob or Obi) starts new branch + PR per § 9.3.

---

## Appendix A — Living Decisions

Decisions made during spec-drafting that may want propagation to other docs:

1. **AD-X (TBD):** Quoted-key repair-vs-skip per skill is determined by the skill's
   own contract, not by a global policy. Repair-skills repair; additive-only
   skills skip-with-finding. The sanity-check is the inversion-of-control point.
2. **Cluster-handling philosophy:** "Defense in depth via per-line regex" — every
   skill that writes YAML calls sanity-check first. If sanity passes, recipes
   are safe. If sanity fails, skill knows what to do (repair or skip) per its
   contract. The cost is one extra read+walk per file; the benefit is no more
   F8/F15/F19/F25/F26-class bugs.
3. **Test-fixture-as-spec:** `test-data/<id>-repro.md` files are not just tests
   — they are the canonical reproductions. The expected/ golden files document
   the post-skill state precisely. Future bug discoveries → new fixture, new
   expected, regression-locked.

These are nominees for `docs/reference/decisions-architecture.md` (in
omnopsis-planning) — propagation owned by MASCHIN, not by this PR.

---

## Appendix B — Sketch of Implementation Order (one-session path, P0 only)

For an implementer working from this spec:

1. Branch: `bob/2026-04-29-v0.1.3-impl` (or obi/, per AD-21 review).
2. Write `references/yaml-sanity.md` per § 5.1.
3. Append recipe (f) to `references/yaml-edits.md` per § 5.2.
4. Update `skills/property-enrich/SKILL.md` per § 8.1 (P0 — Step 2a, sanity-check pre-Write, F2 Source Hierarchy).
5. Update `skills/property-describe/SKILL.md` per § 8.2 (P0 — Step 2a sanity-check, sanity pre-Write, F7 source-policy).
6. Update `skills/note-rename/SKILL.md` per § 8.3 (P0 — Step 4a generalize via sanity-check + recipe f).
7. Update `skills/inbox-sort/SKILL.md` per § 8.4 (P0 — Step 5a generalize via sanity-check + recipe f).
8. Write `test-data/f19-repro.md`, `f25-repro.md`, `f26-repro.md`, `f2-repro.md` per § 6.
9. Write `test-data/expected/<skill>/<fixture>.md` golden files per § 6.5.
10. Write `scripts/smoke-test.sh` per § 7.
11. Bump `plugin.json` + `.claude-plugin/marketplace.json` versions: 0.1.2 → 0.1.3.
12. Update `logs/changelog.md` with release notes.
13. Update `ROADMAP.md` (move v0.1.3 to "current", v0.1.2 to "previous").
14. Run `scripts/smoke-test.sh` manually against fixtures.
15. Commit + push + open PR.

P1 items in a separate commit-block (still on same PR) or deferred to v0.1.3.1 PR.

---

## Appendix C — Glossary

| Term | Meaning |
|------|---------|
| **Quoted-key** | YAML key wrapped in double-quotes WITH trailing colon inside quotes (e.g. `"created:"`). Apple-Notes-Importer-Vintage artifact. |
| **F26-Cluster** | The set of files in a vault with one or more quoted-keys. 5.9% of GR-2 inbox-tree (60 of 1016 files). Cross-skill bug surface. |
| **Sanity-check** | Pre-write step-zero call to `references/yaml-sanity.md`. Returns verdict (OK / BROKEN_KEYS / MULTIPLE_FRONTMATTER_BLOCKS / UNCLOSED / INVALID). |
| **Recipe (f)** | The new normalize-quoted-keys recipe in `references/yaml-edits.md`. Walks lines, replaces quoted-key with plain-key, resolves duplicates. |
| **Repair-skill** | A skill whose contract allows YAML edits beyond its primary write (Nahbereich). property-enrich, note-rename, inbox-sort. |
| **Read-only-property-add skill** | A skill whose contract forbids edits beyond its specific property. property-describe (writes only `description`), property-classify (writes only `status`/`type`). |
| **Cycle-4-USER-PASS** | The pre-launch gate (per Master Plan §7). All 4 GRs PASS + 0 Class-A + all findings triaged. v0.1.3 is now a prerequisite. |
| **Defense in depth** | Calling sanity-check at multiple workflow points (pre-Filter AND pre-Write) so a regression in one path is caught by the other. |

---

*End of v0.1.3 Spec. Implementation begins after user acceptance per § 14.*
